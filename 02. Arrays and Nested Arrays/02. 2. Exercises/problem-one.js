

// const solve = (array, delimeter) => array.join(delimeter);

function solve(array, delimeter) {
    return array.join(delimeter);
}

console.log(solve(['One', 
'Two', 
'Three', 
'Four', 
'Five'], 
'-'
));
